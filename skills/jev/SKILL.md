---
name: jev
description: Alias for /customs — the Customs Authority front desk for TypeSafe's Jev (System One). USE WHEN user types /jev, "jev", "jev help", "jev setup", "jev status", "jev tutorial", or "jev expert". Same subcommands, same behaviour.
---

# /jev → /customs

This is an alias. Follow `${CLAUDE_PLUGIN_ROOT}/skills/customs/SKILL.md` exactly, mapping
`/jev <sub>` to `/customs <sub>` (`help`, `setup`, `status`, `tutorial`, `expert`).
